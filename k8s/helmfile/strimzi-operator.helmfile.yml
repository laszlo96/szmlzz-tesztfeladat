repositories:
  - name: strimzi
    url: https://strimzi.io/charts

releases:
  - name: strimzi-kafka-operator
    namespace: kafka
    createNamespace: true
    chart: strimzi/strimzi-kafka-operator
    version: "0.40.0"
    values:
      - values/{{ .Environment.Name }}/strimzi-operator.yaml
