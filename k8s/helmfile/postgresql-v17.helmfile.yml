repositories:
  - name: cnpg
    url: https://cloudnative-pg.github.io/charts

releases:
  - name: cnpg-cluster-v17
    namespace: cnpg-{{ .Environment.Name }}
    createNamespace: false
    chart: cnpg/cluster
    version: "0.3.0"
    values:
      - values/{{ .Environment.Name }}/postgresql-v17.yaml
