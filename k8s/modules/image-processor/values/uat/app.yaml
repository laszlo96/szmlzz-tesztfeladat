replicaCount: 1
springProfile: uat

image:
  repository: <ecr-registry>/image-processor
  tag: latest

resources:
  requests:
    cpu: 250m
    memory: 256Mi
  limits:
    cpu: 1000m
    memory: 512Mi

autoscaling:
  enabled: false

ingress:
  enabled: true
  host: image-processor.uat.example.com

kafka:
  bootstrapServers: kafka-kafka-bootstrap.kafka.svc.cluster.local:9092
  topic: image-jobs-uat
  groupId: image-processor-uat

redis:
  master: mymaster
  sentinels: redis-sentinel.redis.svc.cluster.local:26379

db:
  host: cnpg-cluster-rw.cnpg-uat.svc.cluster.local
  name: imageprocessor
  user: imageprocessor

s3:
  bucket: image-processor-uat

aws:
  region: eu-central-1

serviceAccount:
  iamRoleArn: arn:aws:iam::123456789:role/image-processor-uat
