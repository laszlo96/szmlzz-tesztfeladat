replicaCount: 2
springProfile: prod

image:
  repository: <ecr-registry>/image-processor
  tag: latest

resources:
  requests:
    cpu: 500m
    memory: 512Mi
  limits:
    cpu: 2000m
    memory: 1Gi

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 10
  targetCPU: 70
  targetMemory: 80

ingress:
  enabled: true
  host: image-processor.example.com

kafka:
  bootstrapServers: kafka-kafka-bootstrap.kafka.svc.cluster.local:9092
  topic: image-jobs
  groupId: image-processor-prod

redis:
  master: mymaster
  sentinels: redis-sentinel.redis.svc.cluster.local:26379

db:
  host: cnpg-cluster-rw.cnpg-prod.svc.cluster.local
  name: imageprocessor
  user: imageprocessor

s3:
  bucket: image-processor-prod

aws:
  region: eu-central-1

serviceAccount:
  iamRoleArn: arn:aws:iam::123456789:role/image-processor-prod
