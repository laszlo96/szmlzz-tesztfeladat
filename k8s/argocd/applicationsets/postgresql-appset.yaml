apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: cnpg-cluster
  namespace: argocd
spec:
  generators:
    - list:
        elements:
          - env: dev
            cluster: https://dev.eks.example.com
          - env: uat
            cluster: https://uat.eks.example.com
          - env: staging
            cluster: https://staging.eks.example.com
          - env: prod
            cluster: https://prod.eks.example.com
  template:
    metadata:
      name: cnpg-cluster-{{ env }}
    spec:
      project: default
      source:
        repoURL: https://cloudnative-pg.github.io/charts
        chart: cluster
        targetRevision: "0.3.0"
        helm:
          valueFiles:
            - https://raw.githubusercontent.com/your-org/your-repo/HEAD/k8s/argocd/values/{{ env }}/postgresql.yaml
      destination:
        server: "{{ cluster }}"
        namespace: cnpg-{{ env }}
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
